<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bookkeeping.product.type.mapper.BizProductTypeMapper">

    <resultMap id="BizProductTypeResultMap" type="com.example.bookkeeping.product.type.entity.BizProductType">
        <id column="id" property="id"/>
        <result column="type_code" property="typeCode"/>
        <result column="type_name" property="typeName"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="BaseColumns">
        id, type_code, type_name, parent_id, sort_order, status, remark,
        created_at, updated_at, deleted
    </sql>

    <select id="selectById" resultMap="BizProductTypeResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM biz_product_type
        WHERE id = #{id}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByTypeCode" resultMap="BizProductTypeResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM biz_product_type
        WHERE type_code = #{typeCode}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectAll" resultMap="BizProductTypeResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM biz_product_type
        WHERE deleted = 0
        ORDER BY parent_id ASC, sort_order ASC, id ASC
    </select>

    <select id="countChildren" resultType="int">
        SELECT COUNT(1)
        FROM biz_product_type
        WHERE parent_id = #{parentId}
          AND deleted = 0
    </select>

    <insert id="insert" parameterType="com.example.bookkeeping.product.type.entity.BizProductType"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_product_type (
            type_code, type_name, parent_id, sort_order, status, remark,
            created_at, updated_at, deleted
        ) VALUES (
            #{typeCode}, #{typeName}, #{parentId}, #{sortOrder}, #{status}, #{remark},
            NOW(), NOW(), 0
        )
    </insert>

    <update id="update" parameterType="com.example.bookkeeping.product.type.entity.BizProductType">
        UPDATE biz_product_type
        SET type_code = #{typeCode},
            type_name = #{typeName},
            parent_id = #{parentId},
            sort_order = #{sortOrder},
            status = #{status},
            remark = #{remark},
            updated_at = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE biz_product_type
        SET deleted = 1,
            updated_at = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>
</mapper>
